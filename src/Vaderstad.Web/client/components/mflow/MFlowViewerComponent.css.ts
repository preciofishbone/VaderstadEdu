import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the MFlowViewerComponent component
 */
export const MFlowViewerComponentStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(MFlowViewerComponentStyles, {
    container: {
        fontSize: 22
    }
})

