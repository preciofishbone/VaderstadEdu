import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the MediaPickerProvider component
 */
export const MediaPickerProviderStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(MediaPickerProviderStyles, {
    container: {
        fontSize: 22
    }
})

